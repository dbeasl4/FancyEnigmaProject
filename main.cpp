#include <iostream>
#include <chrono>
#include "Enigma.cpp"
void handleUserInput(char rawInputTime, Enigma& machine) {

}
int main() {
    auto lastInputTime = std::chrono::steady_clock::now();

    return 0;
}